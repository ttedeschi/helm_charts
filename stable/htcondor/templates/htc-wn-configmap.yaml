{{- if .Values.htcWn.siteConfCMS.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: siteconfig
data:
  {{- range  .Values.htcWn.siteConfCMS.files }}
  {{ .filename }}: |
    {{ .content }}
  {{- end }}

{{- end }}