# Default values for jupyterhub.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

iam:
    endpoint: https://iam.cloud.infn.it

hub:
    host: localhost
    port: 30888
    image: dodasts/jupyter-hub:v3.0.0

s3:
    enabled: false